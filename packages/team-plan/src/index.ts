/**
 * `@bombfarm/team-plan` public root.
 *
 * A host owes this package five things: supply the flat inputs record and the controls
 * record; supply the controls' storage; supply the host-copy contract; supply the
 * empty-state panels; and never render a stale plan unlabelled.
 */
