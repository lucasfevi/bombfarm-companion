---
"@bombfarm/desktop": minor
---

Add a support link to the desktop app, in the two shapes the web planner already uses: an
icon-only pill in the top bar beside the language toggle, and a labelled button in a new
Settings section. Both open the page in the default browser rather than inside the app.
