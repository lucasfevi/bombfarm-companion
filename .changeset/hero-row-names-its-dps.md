---
"@bombfarm/team-plan": patch
"@bombfarm/web": patch
"@bombfarm/desktop": patch
---

The Optimizer's per-hero rows label their three figures "DPS before", "DPS after" and "Δ DPS", so the number is named where it is read rather than two paragraphs above.
