---
"@bombfarm/domain": patch
---

The one-shot identifier guard's allowlist names the web front page's farm comparison, which reads the ranking board's one-shot column to say whether a phase is one-shot. Test-only; nothing the package ships changes.
