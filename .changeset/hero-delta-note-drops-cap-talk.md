---
"@bombfarm/team-plan": patch
"@bombfarm/web": patch
"@bombfarm/desktop": patch
---

The note over the Optimizer's per-hero table no longer says the figures ignore the game's crit and cooldown caps. They never did — every DPS the search scores runs through those caps; only the Stats panel's raw sheet totals can read past them, and that panel marks a capped line itself.
