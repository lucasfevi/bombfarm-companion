---
"@bombfarm/team-plan": patch
---

Adds an `applyPanel` slot to the team-plan results screen, rendered between the run summary and
the per-hero table. A host that leaves it undefined sees no change — no wrapper, no gap.
