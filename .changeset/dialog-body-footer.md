---
'@bombfarm/ui': minor
'@bombfarm/desktop': patch
---

`Dialog` gains a `Body` that scrolls inside the popup and a `Footer` that pads its own actions and rules a full-width line; the consent modal is drawn with them, with its title in a header band.
