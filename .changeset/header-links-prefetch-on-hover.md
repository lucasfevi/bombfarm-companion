---
"@bombfarm/web": patch
---

Header links prefetch a route's payload when you hover, touch, or focus them instead of on every page load, so a visit no longer downloads every section's payload up front.
