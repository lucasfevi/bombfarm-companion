---
"@bombfarm/domain": patch
---

Adds the commit vector's server index order (`COMMIT_ORDER`/`COMMIT_INDEX`) and
`pointsToCommitVector`, the first piece of the plan-to-calls derivation. Internal — nothing
shipped calls it yet.
