---
"@bombfarm/desktop": patch
---

Add an opt-in Settings switch that asks Steam to start Bomb Farm again if the game process disappears after this app has seen it running. Off by default. Closing the game while the switch is on is treated as an exit to recover from. Companion start never launches the game on its own.
