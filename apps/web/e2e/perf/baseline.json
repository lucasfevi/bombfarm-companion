{
  "captureMode": "prod-profile",
  "fixture": "importedRoster",
  "_note": "REFRESHED (9) for the hero identity panel drawing the hero's ability pool (2026-09-10), per docs/react-performance.md rule 10's deliberate-change path - never by widening the guard, which compares exactly on purpose. Every scenario moved UP, which the previous refresh noted is the direction a render leak moves one, so this was measured rather than argued: P-02 7046->7206 (+160), P-03 12643->12915 (+272), P-04 6922->7034 (+112), P-05 2214->2262 (+48). The cause is one structural addition. The identity panel now renders HeroAbilityIcons under the portrait, and the fixture hero owns two abilities, each wrapped in the design-system tooltip - 8 component paths per ability, so 16 component instances that were not mounted before. Every delta is exactly 16 x that scenario's own commit count: P-02 16x10=160, P-03 16x17=272, P-04 16x7=112, P-05 16x3=48. Read off the capture's own renderTally rather than inferred: filtering the paths that pass through HeroAbilityIcons and under HeroIdentityRollPanel sums to exactly the delta in all four - 160, 272, 112 and 48. P-04 needs that second filter because it opens the hero picker, whose own rows have carried an abilities column all along; those 215 renders are pre-existing and are not part of the delta. A constant instance count across four scenarios with different interactions and different commit counts is the signature of a structural addition; a leak scales with what invalidates, not with commits. The PR's other web-facing changes add no components: the Preview column and the reset-advice line are withdrawn only on a read-only host, the gear comparison only where no second loadout can exist, and the unit change puts a suffix on figures already rendered. Captured on the host with PERF_FORCE=1 after perf:build:profile, all four deterministic across 7 repetitions, and the four medians reproduce this PR's CI run exactly.",
  "scenarios": [
    {
      "id": "P-02",
      "medianComponentRenders": 7206
    },
    {
      "id": "P-03",
      "medianComponentRenders": 12915
    },
    {
      "id": "P-04",
      "medianComponentRenders": 7034
    },
    {
      "id": "P-05",
      "medianComponentRenders": 2262
    }
  ]
}
