{
  "captureMode": "prod-profile",
  "fixture": "importedRoster",
  "_note": "REFRESHED (5) for the Optimize build target select (2026-08-31), per docs/react-performance.md rule 10's deliberate-change path - never by widening the guard, which compares exactly on purpose. Every scenario moved UP and none moved down: P-02 6778->6907 (+129), P-03 12240->12445 (+205), P-04 6741->6832 (+91), P-05 2139->2178 (+39). One cause: the Points panel gained a Base UI Select glued to its Optimize build button, plus one more PointsPreviewNotice line. Tabs render every panel, so the Points panel commits on every pass whatever tab is active - which is why all four scenarios moved, including the three that never open it. Divide each delta by REFRESHED (4)'s own one-component figures and the four quotients land at 12.9 / 12.1 / 13.0 / 13.0: no scenario is out of line with the others, which is what says the added control group is the WHOLE cause rather than a render leak riding along with it. Captured on the host with PERF_FORCE=1 after perf:build:profile at a08efe24; all four scenarios reported deterministic=true, and the four figures reproduce the CI run's own numbers exactly (job 99566343079), which is what makes this a safe re-pin rather than a machine-local one. REFRESHED (4) for the download page's header button (2026-08-31), per docs/react-performance.md rule 10's deliberate-change path — never by widening the guard, which compares exactly on purpose. Every scenario moved UP and none moved down: P-02 6768->6778 (+10), P-03 12223->12240 (+17), P-04 6734->6741 (+7), P-05 2136->2139 (+3). One cause: the shell header gained a primary Download link beside Import, so every header pass commits one more component than it used to. None of these four scenarios visits /download — they pay only for that component, which is why each moved by an amount proportional to the header passes it drives (P-03 drives the most and moved the most, P-05 the fewest and least), and why these four deltas are IDENTICAL to REFRESHED (3)'s: one more component in the header costs exactly what one more nav item cost. That identity is the evidence that the button is the WHOLE cause — a render leak anywhere else in the branch would have moved one scenario out of line with the others. Captured on the host with PERF_FORCE=1 after perf:build:profile; all four scenarios reported deterministic=true, and the four figures reproduce the CI run's own numbers exactly (job 99366028993), which is what makes this a safe re-pin rather than a machine-local one. REFRESHED (3) for the Inventory route (2026-08-27), per docs/react-performance.md rule 10's deliberate-change path — never by widening the guard, which compares exactly on purpose. Every scenario moved UP and none moved down: P-02 6758->6768 (+10), P-03 12206->12223 (+17), P-04 6727->6734 (+7), P-05 2133->2136 (+3). One cause: the shell's AppNav gained a fifth item, so every header pass commits one more nav item than it used to. None of these four scenarios touches the Inventory screen — they pay only for the nav item, which is why each moved by an amount proportional to the header passes it drives (P-03 drives the most and moved the most, P-05 the fewest and least), and why the four deltas hold the same ratio to each other that REFRESHED (2)'s did. That uniformity is the evidence that the nav item is the WHOLE cause: a render leak anywhere else in the branch would have moved one scenario out of line with the others. Captured on the host with PERF_FORCE=1 after perf:build:profile; all four scenarios reported deterministic=true, and the four figures reproduce the CI run's own numbers exactly (job 98691604735), which is what makes this a safe re-pin rather than a machine-local one. REFRESHED (2) for the shared top bar (2026-08-26), per docs/react-performance.md rule 10's deliberate-change path — never by widening the guard, which compares exactly on purpose. Every scenario moved DOWN and none moved up: P-02 6778->6758 (-20), P-03 12240->12206 (-34), P-04 6741->6727 (-14), P-05 2139->2133 (-6). One cause: the header's four NavLink components became a single AppNav that renders its items inline, and the two language buttons became a single SegmentedToggle, so a header pass commits five components where it used to commit ten. The saving is per pass, which is why the scenario that drives the most passes (P-03) drops the most. Captured on the host with PERF_FORCE=1 after perf:build:profile; all four scenarios reported deterministic=true, and the four figures reproduce the CI run's own numbers exactly (job 98371569093), which is what makes this a safe re-pin rather than a machine-local one. REFRESHED (1) for the Account page (2026-08-22), per docs/react-performance.md rule 10's deliberate-change path — never by widening the guard, which compares exactly on purpose. Every scenario moved DOWN and none moved up: P-02 7538->6778 (-760), P-03 13532->12240 (-1292), P-04 7280->6741 (-539), P-05 2371->2139 (-232). One cause: the Account panel left the planner's tab strip for its own route, so AccountColumn and its four field groups (house, farm target, skill tree, team buffs) no longer mount inside the tab panels and no longer re-render on every planner interaction. P-05 (switch planner tab) drops for the additional reason that there are three tabs to cycle instead of four. Captured on the host with PERF_FORCE=1 after perf:build:profile; all four scenarios reported deterministic=true, and the four figures reproduce the CI run's own numbers exactly (job 97048210106), which is what makes this a safe re-pin rather than a machine-local one.",
  "scenarios": [
    {
      "id": "P-02",
      "medianComponentRenders": 6907
    },
    {
      "id": "P-03",
      "medianComponentRenders": 12445
    },
    {
      "id": "P-04",
      "medianComponentRenders": 6832
    },
    {
      "id": "P-05",
      "medianComponentRenders": 2178
    }
  ]
}
