export { HeroPickerDialog } from './components/hero-picker-dialog';
// The toggle itself is `@bombfarm/hero`'s — the hero picker's rows carry it and moved with them.
// Re-exported here so the planner strip's own call site is unchanged.
export { HeroActiveToggle } from '@bombfarm/hero/components';
