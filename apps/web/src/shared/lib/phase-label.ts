/**
 * Re-export — the definition lives in `@bombfarm/farm`, which the farm screen and this app's
 * Account page header both print through. Kept here so the Account page's import path is
 * unchanged and there is still exactly one definition.
 */
export { formatPhaseLabel } from '@bombfarm/farm';
