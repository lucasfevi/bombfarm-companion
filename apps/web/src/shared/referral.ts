/**
 * The maintainer's in-game referral code, surfaced in the footer.
 *
 * Both sides receive a reward once the invited player clears stage 151, so the
 * footer copy states the mutual benefit rather than presenting it as a one-way
 * favour. Update here — the footer is the only consumer.
 */
export const REFERRAL_CODE = 'F-S7999UQC';
